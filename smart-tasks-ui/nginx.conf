# smart-tasks-ui/nginx.conf
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        # For SPA routing – if file not found, serve index.html
        try_files $uri /index.html;
    }
}
